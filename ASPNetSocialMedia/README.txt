To run this application, first select the project folder
and enter this line in the terminal:

dotnet user-secrets set SeedUserPW SuperTeam1!

This will seed the secret admin password to the database.
The password is SuperTeam1!
To select the right folder, you may have to enter:

cd ASPNetSocialMedia


