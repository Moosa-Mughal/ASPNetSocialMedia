@model IEnumerable<ASPNetSocialMedia.Models.FriendRelation>

@{
    ViewData["Title"] = "Index";
}

<h1>Friend List</h1>

<p>
    <a asp-action="Create">Add a new friend!</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FriendEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FriendEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FriendRelationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FriendRelationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FriendRelationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
